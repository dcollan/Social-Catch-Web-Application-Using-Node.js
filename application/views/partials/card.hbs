<div id="post-{{id}}" class="card">
    <img class ="card-image" src="{{thumbnail}}" alt="Image is missing">
    <div class ="card-body">
        <p class="card-title">{{title}}</p>
        <p class="card-text">{{description}}</p>
        <a href="/post/{{id}}" class="anchor-buttons">Image Information</a>
    </div>
</div>